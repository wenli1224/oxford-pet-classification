# 基于深度学习的牛津宠物细粒度分类实验报告

## 1. 任务背景与实验设置

本项目面向宠物品种的细粒度图像分类，使用 Oxford-IIIT Pet 数据集，共 **37 类、7349 张图片**。任务需要区分外观接近的宠物品种，对面部、耳部等局部特征的辨别能力提出了较高要求。

实验合并官方 `trainval` 与 `test`，采用 **Stratified Split（分层划分）**，按 **70% 训练、15% 验证、15% 测试**重新划分。实际样本数为 5144、1102、1103；分层划分尽量保持各类别的比例，固定 **seed=42**，两次划分均使用 `random_state=42`。验证集用于选择最佳模型，测试集用于最终性能评估。

模型采用 **ImageNet 预训练 ResNet-18**，将最后的全连接层替换为 37 类输出。Baseline 对全部参数进行微调，使用 **AdamW** 优化器，学习率 **lr=1e-4**，权重衰减 `weight_decay=1e-4`，**batch size=32、epochs=10**；损失函数为交叉熵，默认不启用标签平滑。训练过程中保存验证准确率最高的模型，并使用该模型进行测试。

输入图像裁剪为 224×224，预处理设置如下：

- **训练集：**`Resize(256)` → `RandomCrop(224)` → `RandomHorizontalFlip()` → `ToTensor()` → `Normalize`。随机裁剪与水平翻转用于数据增强。
- **验证集与测试集：**`Resize(256)` → `CenterCrop(224)` → `ToTensor()` → `Normalize`，不使用随机裁剪或随机翻转。

`Normalize` 使用 ImageNet 的均值 `(0.485, 0.456, 0.406)` 和标准差 `(0.229, 0.224, 0.225)`。所有实验均在 Kaggle GPU 环境实际运行。

## 2. 实验结果与消融分析

实验比较 Baseline、标签平滑系数为 0.1 的 Label Smoothing，以及冻结骨干参数、仅训练 `fc` 层的 Freeze Backbone。测试准确率衡量整体分类表现；Macro-F1 对各类别的 F1 等权平均，用于评估不同品种的综合识别效果。

| 实验 | Best Val Accuracy | Test Accuracy | Macro-F1 | Training Time |
| --- | ---: | ---: | ---: | ---: |
| Baseline | **93.01%** | **91.75%** | **0.9172** | 3.59 min |
| Label Smoothing 0.1 | — | 91.66% | 0.9161 | — |
| Freeze Backbone | — | 88.30% | 0.8814 | — |

注：以上数值来自实际运行记录；“—”表示未提供该项记录。

- **Baseline 表现最好。**其测试准确率与 Macro-F1 均为三组实验中的最高值，最佳验证准确率达到 93.01%，训练用时为 3.59 min，说明预训练模型经过全参数微调后能够较好地适应本任务。
- **Label Smoothing 几乎无明显收益。**相较 Baseline，测试准确率降低 0.09 个百分点，Macro-F1 降低 0.0011，两个指标基本接近。在本次实验设置下，标签平滑未带来明显改进。
- **Freeze Backbone 性能明显下降。**测试准确率降低 3.45 个百分点，Macro-F1 降低 0.0358。仅调整分类层对细微品种差异的适应能力不足，结果说明全参数微调更适合当前细粒度分类任务。

因此，在本次固定划分与超参数设置下，选择 Baseline 作为最终模型。

## 3. 错误案例分析与 Grad-CAM

测试集混淆统计中的部分主要误分类如下，方向为“真实类别 → 预测类别”：

| 真实类别 | 预测类别 | 误分类数量 |
| --- | --- | ---: |
| American Pit Bull Terrier | Staffordshire Bull Terrier | 8 张 |
| Chihuahua | Miniature Pinscher | 6 张 |
| Egyptian Mau | Bengal | 6 张 |
| Staffordshire Bull Terrier | Boxer | 3 张 |
| Ragdoll | Persian | 3 张 |

这些混淆类别的外观较为相似，面部轮廓和耳部特征接近，容易造成误分类。相较于区分猫与狗等较粗的类别，品种识别需要利用更细致的局部差异；当图像中这些差异不够明显时，模型可能将相近品种混淆。

为分析模型的关注区域，使用 Grad-CAM 对 ResNet-18 最后一个卷积块 `model.layer4[-1]` 进行可视化，并将热力图叠加到原图上。实际观察结果如下：

- **正确样本：**模型主要关注宠物头部、脸部和耳朵区域，这些区域包含较多与品种识别相关的外观信息。
- **错误样本：**一个真实类别为 **American Bulldog** 的样本被预测为 **Staffordshire Bull Terrier**。其热力区域主要集中在头部和面部，模型虽然关注到了宠物本体，但仍未充分区分相似品种。

上述结果提示模型主要依据局部外观特征进行判断，与混淆统计中相似品种易被误判的现象一致。Grad-CAM 为理解预测提供了辅助依据，也说明关注头部和面部并不必然保证分类正确。

## 4. AI 辅助编程声明与 Bug 复盘

本项目使用 AI 辅助生成数据加载、训练循环、Grad-CAM 和工程结构代码，并辅助排查错误、整理项目说明。**所有实验结果均来自 Kaggle GPU 的实际训练与测试运行**，报告中的指标、混淆计数及可视化观察依据实际实验记录整理，未使用 AI 模拟实验数据。

实验过程中出现过一个真实 Bug：在 Kaggle Notebook 运行数据划分代码时，报错 `NameError: trainval_dataset is not defined`。

- **原因：**当前 Kaggle Session 中，前一个 OxfordIIITPet 数据下载代码单元未执行，或 Session 重启后变量已丢失，导致数据划分单元无法访问 `trainval_dataset`。
- **解决：**重新执行 OxfordIIITPet 数据下载与数据集实例创建代码，恢复 `trainval_dataset` 等变量后，再运行数据划分代码。
- **复盘：**该问题源于 Notebook 的执行顺序与变量状态依赖。后续应按“数据下载与加载 → 数据划分 → 模型训练 → 测试评估”的顺序执行；Session 重启后需重新运行前置单元，避免仅执行后续单元而缺失必要变量。
